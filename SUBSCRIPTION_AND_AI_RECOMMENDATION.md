# 订阅和 AI 推荐功能介绍

## 1. 订阅功能

### 1.1 功能说明
订阅功能允许用户订阅特定的充电桩，当充电桩从无空闲变为有空闲时，系统会发送通知提醒用户。

### 1.2 实现逻辑

#### 1.2.1 订阅流程
1. **用户选择充电桩**：用户在小程序中选择一个或多个充电桩进行订阅
2. **调用订阅云函数**：前端调用 `subscribe` 云函数，传递模板ID和充电桩ID列表
3. **记录订阅信息**：系统在 `subscribes` 集合中创建或更新订阅记录
4. **记录用户行为**：调用 `recordChargingHistory` 云函数，记录用户的订阅行为到 `chargingHistory` 集合

#### 1.2.2 状态检测
1. **定时检测**：系统通过 `checkStationStatus` 云函数定时检测充电桩状态
2. **状态对比**：比较当前状态与历史状态，判断是否从无空闲变为有空闲
3. **发送通知**：如果状态变化满足条件，发送模板消息通知用户
4. **记录状态**：将充电桩状态记录到 `stationHistory` 集合

#### 1.2.3 特定时间点记录
系统会在每天的以下时间点记录所有充电桩的状态到 `stationStatusHistory` 集合：
- 7:30
- 10:30
- 13:30
- 16:30
- 19:30
- 22:30

## 2. AI 推荐功能

### 2.1 功能说明
AI 推荐功能基于用户的历史行为和充电桩的历史状态数据，为用户推荐最适合的充电桩。

### 2.2 实现逻辑

#### 2.2.1 数据收集
1. **用户行为数据**：从 `chargingHistory` 集合中获取用户的点击、订阅和充电历史
2. **充电桩状态数据**：从 `stationStatusHistory` 集合中获取过去7天所有充电桩的状态历史

#### 2.2.2 数据处理
1. **用户偏好计算**：
   - 分析用户的时间偏好（preferredTimeSlots）
   - 分析用户的校区偏好（preferredCampuses）
   - 分析用户的充电桩偏好（preferredStations）

2. **充电桩状态分析**：
   - 计算每个充电桩在每个时间点的平均空闲数和总数
   - 分析充电桩的使用趋势

#### 2.2.3 AI 推荐
1. **准备输入数据**：将用户偏好和充电桩状态数据整合成 AI 输入格式
2. **调用 AI 模型**：使用 AI 模型分析数据，生成推荐结果
3. **返回推荐**：将 AI 推荐的充电桩列表返回给用户

### 2.3 数据流程图

```
用户行为数据（点击、订阅、充电） ---> chargingHistory 集合
                                    |
                                    v
                                getRecommendationData 云函数 <--- 充电桩状态数据（每3小时记录）
                                    |                           |
                                    v                           v
                                数据处理和分析             stationStatusHistory 集合
                                    |
                                    v
                                生成 AI 输入数据
                                    |
                                    v
                                调用 AI 模型
                                    |
                                    v
                                返回推荐结果
```

## 3. 技术实现要点

### 3.1 云函数
- **subscribe**：处理用户订阅请求，记录订阅信息
- **recordChargingHistory**：记录用户行为（点击、订阅、充电）
- **checkStationStatus**：检测充电桩状态，发送通知，记录状态历史
- **getRecommendationData**：获取和处理数据，准备 AI 输入

### 3.2 数据库集合
- **subscribes**：存储用户订阅信息
- **chargingHistory**：存储用户行为历史
- **stationHistory**：存储用户订阅的充电桩状态历史
- **stationStatusHistory**：存储所有充电桩的状态历史

### 3.3 定时任务
系统需要配置定时任务，定期调用 `checkStationStatus` 云函数，以确保及时检测充电桩状态和记录历史数据。

## 4. 使用建议

1. **合理设置订阅**：用户应根据自己的需求选择合适的充电桩进行订阅
2. **定期检查推荐**：系统会根据用户行为和充电桩状态的变化更新推荐结果，用户应定期查看
3. **反馈优化**：用户的每一次点击和订阅都会影响推荐结果，系统会不断学习和优化推荐算法

## 5. 总结

订阅功能和 AI 推荐功能相结合，为用户提供了更加便捷、个性化的充电桩使用体验。通过记录用户行为和充电桩状态数据，系统能够智能地为用户推荐最适合的充电桩，同时在充电桩空闲时及时通知用户，提高充电桩的使用效率。